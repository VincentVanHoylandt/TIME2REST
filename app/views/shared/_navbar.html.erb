<div class="nav-container" data-hero-target="bar">

  <%= render partial: 'shared/logo' %>
  <%= render partial: 'shared/search_bar', locals: {path: offers_path} %>

  <div class= "nav-btn-dshb-container">

    <% if user_signed_in? %>

      <%= link_to 'New Offer', new_offer_path, class: "my-btn" %>
      <%= link_to dashboard_path, class:"my-dashboard-btn" do  %>
        <i class="fa-solid fa-bars"></i>
      <%end%>

      <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete} %>

    <% else %>
      <%= link_to 'New Offer', new_user_session_path, class: "my-btn" %>
      <%= link_to new_user_session_path, class:"my-dashboard-btn" do  %>
        <i class="fa-solid fa-bars"></i>
      <%end%>
    <% end %>

  </div>

</div>
